#!/usr/bin/env bash
# using if-else in loops

a=0
while [ $a -lt 10 ]
do
	if [ $a -eq 3 ]
	then
		echo "bad luck"
	elif [ $a -eq 7 ]
	then
		echo "good luck"
		continue
	else
		echo "Best School"
	fi
	(( a++ ))
done
